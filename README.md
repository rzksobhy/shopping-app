# shooping-app

This is a project I'm working on to practice the MERN stack

# Getting started

if you want to get started with this project follow these steps

- Setting up the client
```bash
cd client && yarn install && yarn run build
```

- Setting up the server
```bash
cd server && yarn install && yarn run build
```
